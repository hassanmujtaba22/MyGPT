# Core fine-tuning stack
# Tested with Python 3.10–3.12

torch>=2.3.0
transformers>=4.44.0
datasets>=2.20.0
peft>=0.12.0
trl>=0.9.6
accelerate>=0.33.0

# 4-bit quantization (QLoRA). NVIDIA GPU only.
# On Apple Silicon / CPU, bitsandbytes is unavailable — training scripts will
# fall back to non-quantized LoRA automatically (needs more memory).
bitsandbytes>=0.43.0 ; platform_system == "Linux" or platform_system == "Windows"

# Config + utilities
pyyaml>=6.0
sentencepiece>=0.2.0
protobuf>=4.25.0

# RAG: local embeddings + document retrieval
sentence-transformers>=3.0.0
numpy>=1.24.0
pypdf>=4.0.0            # read PDFs in your knowledge folder (optional)

# Optional: FAISS for very large document sets (the default NumPy store is fine
# for thousands of chunks). Uncomment if you need to scale up.
# faiss-cpu>=1.8.0

# Web UI (scripts/serve.py). The Ollama backend uses only the Python stdlib,
# so a CPU/Mac user can run the UI without the heavy ML stack above.
fastapi>=0.110.0
uvicorn>=0.29.0
pydantic>=2.0.0

# Optional: faster attention on supported NVIDIA GPUs (uncomment if desired)
# flash-attn>=2.5.0
